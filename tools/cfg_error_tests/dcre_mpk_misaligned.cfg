/*
 *  DEF_MPK に非NULLのmpkを与えつつ、mpkszをCHECK_MB_ALIGN（musca_b1系は4）
 *  に合致しない値にすると E_PAR（mpkszが not aligned）
 *
 *  INCLUDEパス／EXTRA_CFLAGSの事情は dcre_aid_in_class.cfg と同じ。
 */
INCLUDE("test/test_common1.cfg");
#include "test_int2.h"
CLASS(CLS_PRC1) {
	CRE_TSK(TASK1, { TA_ACT, 1, task1, MID_PRIORITY, STACK_SIZE, NULL });
}
DEF_MPK({ 4097, (void *)0x20000000 });
