/*
 *  AID_TSK(1) のみで CRE_TSK が1個も無い構成（★訂正E ガードの波及検証）
 *
 *  訂正E ガードは共通 KernelObject.generate() 側に実装されているため、
 *  段階1から kernel_api.def に登録済みの AID_TSK にも自動的に効く。
 *  task.py:103-104／task.trb:117-118 の既存チェック（"no task is
 *  registered"、非致命的エラー＝error_flag を立てて処理継続）に続けて、
 *  訂正E ガードの2件目のメッセージ（E_OBJ: AID_TSK requires at least
 *  one CRE_TSK in the system）が出ることを実測する。
 *
 *  dcre_aid_in_class.cfg と同じ理由（cfg_py/cfg.py:search_file_path が
 *  cwd相対でしか解決できず、run.shは常にスクラッチディレクトリへcdする）
 *  で、「test/test_common1.cfg」と書き、実行時に EXTRA_CFLAGS として
 *  「-I<repo>/test」（第4引数）を渡す。
 */
INCLUDE("test/test_common1.cfg");
#include "test_int2.h"
AID_TSK(1);
