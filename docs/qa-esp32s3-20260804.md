# esp32_s3 移植側からの照会4件への回答（fmp3_core 側）

回答日: 2026-08-04 / 回答者: fmp3_core `feature/dynamic-creation` 担当
照会元: `esp32_s3/.steering/20260804-dynamic-object-integration/REQUESTS-TO-FMP3CORE.md`
（本文書は fmp3_core 側の記録。esp32_s3 側はポインタで参照のこと）

---

## R-2【最優先・方針確認】自己削除した動的タスクの資源回収 → **(i) 移植側回収の前提**

**カーネル側で `ext_tsk` 時に解放する意図・予定は無い。** 理由は3つ:

1. **dcre 忠実移植の原則**: dcre 自身が ext_tsk で資源を解放しない。回収経路は
   `del_tsk`（他タスク文脈・休止状態必須）のみ。
2. **設計として不可能側に倒してある**: ext_tsk 内での自スタック解放は「自分がまだ
   実行中のスタック」の解放になる。段階1 spec §2.3 に記録した受容済みウィンドウ
   （ext_tsk は release_glock 後も自スタック上で数命令実行する — pristine 継承）が
   既にあり、ext_tsk 内解放はこの窓を「理論上」から「必然」へ広げるため採らない。
3. `del_tsk` の休止必須制約により**自己回収は構造的に不能**（走行中の自分は休止でない）。

貴側の読解（`task_term.c` 差分は TA_NOEXS ガード追加のみ）は**正確**。

**推奨回収パターン**（本リポジトリで QEMU 実証済み）:
`get_tst` で `TTS_DMT` をポーリング → `del_tsk`（`test/test_dcre1.c` シナリオ8。
del_tsk は休止なら任意コアから可能）。reaper でも次回 create 時掃除でも可。

★**設計判断に効く追加情報 — プールは bump アロケータ**（`docs/dynamic-creation.md` §6）:
`free_mpk`（del_tsk 経由）は**割当カウントを減らすだけで brk は戻らない**。
brk が先頭へ戻るのは**カウントが 0（全割当解放）になった瞬間だけ**。
つまり **reaper で回収しても、プール上に他の生存割当（他タスクのスタック・
dtq/pdq/mpf の TA_MBALLOC 管理領域）が 1 つでも残る限り、create/self-delete の
反復でアリーナは単調に痩せ続ける**。回避策の候補:
- **ユーザ供給スタック**（`T_CTSK.stk != NULL`）にする — プールを一切使わず
  TA_MEMALLOC も立たない。貴側の既存静的スロット配列をスタック供給源に転用すれば、
  帳簿は残るがプール劣化は消える（reaper も不要になる可能性がある）
- または「世代」単位で全動的オブジェクトを同時に解放するアプリ設計（count==0 を作る）

---

## R-1【情報】arch 依存関数の一覧 → **必須は USE_TSKINICTXB 時の2関数のみ。他オブジェクトはゼロ**

- **タスク**: `USE_TSKINICTXB` を定義する arch のみ、`init_tskinictxb(TSKINICTXB*, size_t, STK_T*)`
  と `void *tskinictxb_memalloc_ptr(TSKINICTXB*)` の2つが必要。貴側の対処
  （esp32_s3 `9e2ada9`）と規約実測（stk_top=先頭/stk_bottom=先頭+サイズ）は**正しい**。
  なお acre_tsk は `stksz = ROUND_STK_T(stksz)` 後に init_tskinictxb を呼ぶため
  （`kernel/task_manage.c:194-197`）、静的側の `ROUND_STK_T` 済みサイズと規約一致する。
  ※`USE_TSKINICTXB` を使わない arch（riscv 等 — TINIB が stksz/stk を直接持つ）は
  何も不要。
- **sem/flg/mtx/dtq/pdq/mpf**: arch 依存関数の新規要求**なし**（カーネル層で完結）。
- **cyc/alm**: 新規関数なし。既存の `TOPPERS_TEPP_PRC`（target_kernel.h）を参照するのみ
  （静的生成でも必要な既存マクロ。動的 cyc/alm の affinity に使う）。
- **ISR（ENA_DYNISR）**: arch 関数の新規要求**なし**。結合は既存の
  `INT_ENTRY`/`INTHDR_ENTRY` マクロ経由のみ（R-3 参照）。
- **フォールバック群**（arch が定義していればそれが優先・未定義なら kernel 側の
  `#ifndef` 既定が効く）: `TARGET_TSKATR`・`TARGET_MIN_STKSZ`（`kernel/kernel_impl.h`）、
  `CHECK_*_ALIGN`/`CHECK_FUNC_NONNULL` 系（`kernel/check.h` — 未定義なら検査が
  恒真に落ちる。Xtensa で厳密化したければ arch 側で定義）。

恒久文書化: `docs/dynamic-creation.md` に**「移植者向け: arch 層の要件」節を追加した**
（本回答と同じコミット）。以後はそちらが正本。

---

## R-3【制約】Xtensa の結合点2つ → **両方とも保つ。確約する**

1. **`_kernel_inthdr_N` のシグネチャは `void(void)` のまま**（queue-mode でも）。
   現物: `build/kria_r5-2core-tmixma/generated/kernel_cfg.c:262` に
   `void _kernel_inthdr_40(void)` — 中身が `_kernel_call_isr(&_kernel_isr_queue_table[i]);`
   の1行になるだけで、`INT_ENTRY`/`INTHDR_ENTRY` の使用経路・受け渡しは不変。
2. **`INTINIB`（5要素）・`INHINIB`（4要素）の構造・生成は一切変えていない**。
   変える予定も無い。将来変えるときは事前に連絡する。

★**補足への重要な先回り回答（段0測定で必ず見えるもの）**:
「ENA_DYNISR 無効時に生成物が変わらない」は**ディスパッチコードについては保証**
（test_int2 の inthdr バイト不変で実証済み）だが、**kernel_cfg.c 全体のバイト不変では
ない**。AID 系の**恒常出力**（設計裁定・各 spec の「管理された差分」）が、静的オブジェクト
を持つ全構成に出る。段0 で見える追加シンボルの類型:
- 全オブジェクト共通: `TNUM_S*ID` 定義・`_kernel_tmax_s*id`・inib/torder のサイズ
  トークン変更・`TOPPERS_EMPTY_LABEL(*INIB, _kernel_a*inib_table)` 群
- mpk: `_kernel_mpksz = 0` / `_kernel_mpk = NULL`
- ISR（CRE_ISR を1個でも持つ構成 — 貴側は該当）: `_kernel_isrinib_table` /
  `_kernel_isrcb_*` / `_kernel_p_isrcb_table` / `_kernel_isrorder_table` /
  `_kernel_tmax_isrid` 系 / EMPTY_LABEL 群 / 起動時 `_kernel_initialize_isr` 呼出し
- 実測 ROM 影響（musca_b1 sample1）: text +1796 / data +0 / bss +72 バイト

**追加シンボルはすべて `_kernel_` プレフィクスのカーネル帰属**であり、シム／アプリ由来
シンボルの変化は無いはず。貴側のシンボル単位帰属の判定なら「カーネル帰属の追加のみ・
既存シンボルの変化なし」が期待値。もしシム/アプリ帰属の変化が観測されたら、それは
こちらでも説明できない事象なので改めて照会してほしい。
- `DEF_INH` 9本との関係: **ENA_DYNISR を書かない限り一切不変**。同一 intno に
  DEF_INH がある intno への ENA_DYNISR は cfg エラー（E_OBJ）で拒否される。

---

## R-4【確認】→ **貴側の理解は正確。自衛策も的確**

- `TNUM_PRCID == 1` での `mact_tsk(id, 2)` は **E_ID で正しい**
  （現物確認: `kernel/task_manage.c:384` の `CHECK_PRCID` が `:386` の `CHECK_MIG` に
  先行し、`kernel/check.h:340-342` が E_ID を返す）。
- 補足: deferred 項目の対象は「**一度も acre されていないスロット ID の捏造**」のみ。
  acre → del 済み ID への `mact_tsk` は **E_NOEXS が正しく返る**（E_NOEXS ゲートは
  実装・テスト済み）。「acre_tsk が成功して返した ID にしか撃たない」という貴側の
  不変条件で完全にカバーされる。

---

## 現況共有（参考）

- 本質実装・テスト（test_dcre1〜5 + mix/mix_ma）・利用者ドキュメント・最終回帰・
  Codex 外部レビューまで完了。現在 hardening pass（オーバーフロー検査等4件）の
  計画中 — **acre_dtq/pdq/mpf に E_PAR の新検査（サイズ乗算オーバーフロー）が
  入る予定**。API 意味論は変えないが、極端な引数でのエラーコードが E_NOMEM →
  E_PAR に変わるケースが生じ得る。pin 更新時に注意されたい（入り次第この文書の
  末尾に追記する）。

---

## 追記（2026-08-04・hardening pass 確定）E_PAR ガード（4 API・6検査）の確定内容

上記で「入る予定」としていた新検査が確定した（commit `fb5e369`）。**API 意味論は変えず**、
以下の 4 API・6検査が、桁あふれする極端な引数に対して **`E_PAR` を返すように変わった**。
従来の挙動は引数の値域により2通りあった点に注意——積が**大きい側**に回り込む値では
`E_NOMEM` に落ちていたが、積が**小さい値**に回り込む値では**確保が成功して壊れた
オブジェクトができる偽成功**だった（`acre_mpf` の丸めあふれ→`blksz=0` プールは実測で
確認済み。dtq/pdq の積回り込みも同型のハザード）。新ガードはこの両方を `E_PAR` で塞ぐ：

1. **`acre_dtq`**（`kernel/dataqueue.c:379`）: `CHECK_PAR(dtqcnt <= (SIZE_MAX / sizeof(DTQMB)));`
   — `dtqcnt * sizeof(DTQMB)` の桁あふれ検査。
2. **`acre_pdq`**（`kernel/pridataq.c:352`）: `CHECK_PAR(pdqcnt <= (SIZE_MAX / sizeof(PDQMB)));`
   — `pdqcnt * sizeof(PDQMB)` の桁あふれ検査。
3. **`acre_mpf`**（`kernel/mempfix.c:255-257`）: 3本一組
   - `CHECK_PAR(blksz <= (SIZE_MAX - (sizeof(MPF_T) - 1)));`（`:255`、`ROUND_MPF_T(blksz)`
     の丸め加算あふれ）
   - `CHECK_PAR(blkcnt <= (SIZE_MAX / ROUND_MPF_T(blksz)));`（`:256`、ブロック領域確保の
     積あふれ）
   - `CHECK_PAR(blkcnt <= (SIZE_MAX / sizeof(MPFMB)));`（`:257`、管理領域確保の積あふれ）
   - この3本が無いと、`blksz` が丸めであふれて0になった場合に `malloc_mpk(0)` が
     **成功してしまい**、`acre_mpf` が **E_NOMEM を経由せず正常終了**して `blksz=0` の
     壊れたプールができる（実測・変異 control C-2 で確認。`erid` が正の ID で返る偽成功で、
     `E_NOMEM` に「落ちる」よりも悪い失敗モード）。
4. **`acre_tsk`**（`kernel/task_manage.c:184`）: `CHECK_PAR(stksz <= (SIZE_MAX - (sizeof(STK_T) - 1)));`
   — `ROUND_STK_T(stksz)` の丸め加算あふれ検査。`stksz` は `size_t` 幅のため、
   **32bit/64bit のどちらでも到達可能**（他4本と異なり `#if` ガードなしで常に有効）。

**ガードの位置づけ**: 6検査すべて**ロック取得前**（`lock_cpu()`/`acquire_glock()` より前）に置いた、
引数だけから決まるコア非依存の検査であり、既存の `CHECK_VALIDATR`/`MB_ALIGN` 等と同じ
決定的なチェックである（副作用も呼出しタイミング依存も無い）。

**64bit ターゲットでの各検査の生死**（幅ごとの内訳。★初出時の記述を最終レビューで訂正済み）:

- **64bit で恒真（コンパイル時に消える）**: `acre_dtq`/`acre_pdq` の積あふれ検査と
  `acre_mpf` の管理領域積検査（`:257`）。いずれも**除数がコンパイル時定数の
  `sizeof(...)`**で、`uint_t`（32bit固定）のカウントとの積は64bit幅では原理的に
  あふれないため。
- **幅に依存せず両方で有効**: `acre_tsk` の `stksz` 丸めあふれ（`size_t` 幅の引数）、
  `acre_mpf` の `blksz` 丸め加算あふれ（`:255`、同じく `size_t` 幅）、**および
  `acre_mpf` のブロック領域積検査（`:256`）**。最後のものは除数
  `ROUND_MPF_T(blksz)` が**実行時値**なのでコンパイル時に消えることは原理的に
  なく、64bit でも数学的に到達可能（例: `blksz=2^63, blkcnt=2` で64bit積が
  回り込む）。実行時値除算はコンパイラが umull 系の桁あふれ判定へ変換するため、
  `:255` の丸めあふれ検査が先行して除数の 0 回り込みを塞いでいることが前提に
  なっている（3本の順序が意味を持つ）。

**参照コミット**: ガード本体は `fb5e369`（fix(kernel): 動的生成のサイズ乗算あふれ検査と
メモリプールの符号安全化を一体で修正）、runtime 実演（`test/test_dcre1.c`・
`test/test_dcre4.c` への計装、E_PAR が実測で返ることの確認）は `a194928`
（test(dcre): カーネル管理外intpriのENA_DYNISR回帰cfgと、あふれ検査のE_PARをruntimeで実演）。
実演したのは6検査中5検査（dtq積・pdq積・mpf の3本中2本・tsk 丸め）で、
`acre_mpf` の管理領域積検査（`:257`）のみ個別の runtime 実演を持たない
（同型の積検査2本が実演済みのため）。

---

## 追記（2026-08-04・メモリプール freelist 化）R-2 の「単調に痩せる」前提の変更確定

R-2 の「★設計判断に効く追加情報」で述べた **「free_mpk は割当カウントを減らす
だけで brk は戻らない／プール上に他の生存割当が1つでも残る限り create/self-delete
の反復でアリーナは単調に痩せ続ける」は，commit `a32afa1`（feat(kernel):
カーネルメモリプールを完全一致フリーリスト再利用へ置換）で前提が変わった**。

- 新実装：解放済みブロックを freelist に記録し，**要求サイズが完全一致（かつ
  アライン適合）する場合は count==0 を待たず即座に再利用する**。同一 `stksz` の
  タスクや同一諸元の dtq/pdq/mpf を create/delete で回す限り，長寿命割付が共存
  していてもアリーナは痩せない（`test/test_dcre6.c`、commit `b63c7c1` で QEMU
  実証。旧実装では同手順が8周目で必然的に E_NOMEM——変異 control で再現済み）。
- 変わらないもの：`count==0` の全域リセット（backstop）・API・呼出し側・
  `OMIT_MEMPOOL_DEFAULT` フック。**分割・結合・サイズクラス化はしない**
  （異なるサイズの要求は従来どおり bump を前進させる）。
- コスト：割付1件あたり +4B（32bit）／+8B（64bit）のヘッダ，プール制御
  ブロック +4B/+8B。`mpksz` の見積りにはこの分を積むこと。実測 ROM 増分は
  `docs/qa-esp32s3-20260804-3.md` の「現況共有」の節を参照
  （数値をここに重複記載しない）。
- 貴側への含意：R-2 で挙げた回避策（ユーザ供給スタック／世代単位の全解放）は
  引き続き有効だが，「プール利用＝単調劣化」という前提での必須策ではなくなった。
  なお貴側は `_kernel_mpksz = 0`（プール不使用）であり本変更の受益者ではない
  （貴側 REPLY3 の帰属訂正どおり）。設計判断の再考材料として通知する。
